{
  "name": "heartopia-guidebook",
  "version": "1.0.0",
  "private": true,
  "description": "Independent Heartopia fan guide website prepared for Cloudflare Pages and AdSense readiness.",
  "scripts": {
    "dev": "node scripts/prepare-public.mjs && astro dev",
    "build": "node scripts/prepare-public.mjs && astro build",
    "preview": "npx serve dist",
    "validate:data": "node scripts/validate-data.mjs",
    "quality": "node scripts/quality-gate.mjs",
    "qa": "npm run validate:data && npm run build && npm run quality && npm run qa:layout",
    "qa:layout": "node qa/check-layout.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name heartopia-blog"
  },
  "devDependencies": {
    "astro": "^6.4.4",
    "playwright": "^1.60.0",
    "serve": "^14.2.4",
    "typescript": "^6.0.3",
    "wrangler": "^4.19.1",
    "zod": "^4.4.3"
  }
}
