{
  "name": "heartopia-guidebook",
  "version": "1.0.0",
  "private": true,
  "description": "Independent Heartopia fan guide website prepared for Cloudflare Pages and AdSense readiness.",
  "scripts": {
    "dev": "npx serve site",
    "deploy": "npx wrangler pages deploy site --project-name heartopia-demo"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "serve": "^14.2.4",
    "wrangler": "^4.19.1"
  }
}
